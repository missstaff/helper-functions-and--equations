/**
 * calculate average
 */
const calcAverage = (int1, int2, int3) => (int1 + int2 + int3) / numInts;

/**
 * // calculate radius
 */
//Math.PI * 10.0 **2;
Math.PI * Number.parseFloat("10.0px") ** 2;
